syntax = "proto3";

package main;

option go_package = "github.com/rclone/rclone/backend/chain";

message EncryptionAlgo {
    string algo = 1;
    bytes iv = 2;
}

message FileHeader {
    repeated EncryptionAlgo encryptionChain = 1; 
    string compression = 2;
}